# ALIASES
# REM: the functions are defined in .zshrc
# alias chk='cd $HOME && ./info.sh'
# alias contout='$HOME/contact_out.sh' # refers to contacts.csv which is /media/ubuntu/3520-FD13/0My_Folders/00Donaghs_ORG/14_Donaghs_CONTACTS/00_Main_Contacts.csv
# alias ggp='git push git@github.com:Xiddler/xiddler.github.io.git gh-pages:master'
# alias vim='sudo vim '
alias -g C=' | xclip'
alias -g G='| egrep -i --color=auto'
alias -g P='pwd -P'
alias -g PS='ps aux | egrep -i --color=auto'
alias -g catt='cat $HOME/ziim/Computer/11SignUps.txt|rg -i -A5 '
alias -g contin='python3 /media/donagh/3520-FD13/2programming_repository/python/1python_projects/tkinter_GUI/passin_credentials/contin.py' # input new contact
alias -g contout='cat /media/donagh/3520-FD13/zim/ORG/03_CONTACTS/Contacts.txt | ag -i -A18 -B2 '
alias -g exa='~/Applications/exa-linux-x86_64' # modern ls
alias -g kontout='cat /media/donagh/3520-FD13/zim/ORG/03_CONTACTS/Contacts.txt | ag -i -A18 -B2 ' # get contact  
alias -g lsd='du -s -- *(D) | sort -k1n' # list size of directories
alias -g nt='terminator --geometry +500+100' # open a new zsh terminal  
alias -g passin='python3 $HOME/sd64/2programming_repository/python/1python_projects/tkinter_GUI/passin_credentials/pw_data-input.py' # input new password
# alias -g passin='/home/donagh/.scripts/passin.sh' # a non-GUI version
alias -g passout='cat /home/donagh/sd64/zim/Computer/11SignUps.txt|rg -i -A5 ' # alternative alias for catt
alias -g xn='| xargs -n 1'
alias -g zimbu='/home/donagh/.scripts/zim_backup_to_tinyUSB.sh'
alias -g zz='zsh -l'
# 
# alias -g cx='clear'
# the following shortcut alias means that pressing enter after just a 
# file name with .md extension will automagically open it with vim. Hurrah!
alias -s md=vim

# Navigation shortcuts
alias cd..='cd ..'
alias dl='cd ~/Downloads'
alias ds='cd ~/Documents'
alias dr='cd ~/Dropbox'
alias -g sd='cd $HOME/sd64/'
alias -g hf='cd $HOME'
# bindkey 'hf' 'cd $HOME'
alias -g po='cd $HOME/PORTABLE_ENV'
alias -g port='cd $HOME/PORTABLE_ENV'
alias -g tt='cd /home/donagh/TESTING'

# ____________________________________________________________________________________
# shortcuts for when I want to Search Zim - Computer ORG 
alias -g ziim='$HOME/sd64/zim/'
# go to zim/Computer
alias -g szc='cd $HOME/sd64/zim/Computer/'
# go to zim/ORG
alias -g szo='cd $HOME/sd64/zim/ORG/'
# ____________________________________________________________________________________

alias -g sd='cd $HOME/sd64/'
# alias -g hf='cd $HOME'
alias -g 2p='cd $HOME/sd64/2programming_repository/'
alias -g share='cd /home/donagh/linuxShared'
web(){cd $HOME/sd64/2programming_repository/css/css_grid/donagh-dev/}
books() {cd $HOME/sd64/books/Computer_books/ }
bookmarks() { cd /home/donagh/sd64/1donaghs-stuff/0_All_Bookmarks/WebBrowser_Bookmarks }


#-------------------- START journal
# journal='/run/media/donagh/c60cbdfc-37a8-4e08-b2dd-6286d16beb3d/SD35-BACKUP/1donaghs-stuff/personal/journal'
# Go to Current Year/Month 
k=$(date +"%Y/%m")
journal='/home/donagh/sd64/1donaghs-stuff/personal/journal/'$k # short form
personal='/home/donagh/sd64/1donaghs-stuff/personal/' # short form

#
# alias mj=md_journal # opens todays journal as a markdown file in vim for editing.
#
# Functions for shortcuts - equivalent to aliases.
alc() { $EDITOR $personal/minding_me/alcohol_journal.md }
grat() { $EDITOR $personal/minding_me/gratitude_journal.md }
ideas() { $EDITOR $personal/ideas/ideas_to_go_at.md }
insp() { $EDITOR $personal/ideas/inspire.md }
jour() { cd $journal && ls }
# See  alias -g mj=md_journal above
mind() {cd $personal/minding_me }
pain() { $EDITOR $personal/minding_me/pain_points.md }
pers() { cd $personal && ls }
wisd() { $EDITOR $personal/ideas/snippets.md }

#-------------------- END journal
# alias to get my IP address of digital ocean website (2020-02-17)
alias site='dig donaghmccarthy.ie +short'

# dot files shortcuts 
alias cb='cat ~/.bashrc'
alias cv='cat ~/.vimrc'
alias cz='cat ~/.zshrc'
alias cza='cat ~/.zsh_aliases'
alias sb='source ~/.bashrc'
alias sz='source ~/.zshrc'
alias sza='source ~/.zsh_aliases'
alias vb='vim ~/.bashrc'
alias vv='vim ~/.vimrc'
alias vz="vim ~/.zshrc"
alias vza="vim ~/.zsh_aliases"

# Donaghs shortcuts
alias -g chk='$HOME/.scripts/info.sh' # quick summary of system status
alias -g scr='cd $HOME/.scripts/' # cd to /home/donagh/.scripts
# alias -g mj=md_journal # opens todays journal as a markdown file in vim for editing.
alias -g mj='$HOME/.scripts/markdown_journal.sh'
alias -g wcmj='$HOME/.scripts/wordCount_journal.sh' # word count my journal (markdown journal)
alias hanging='cd $HOME/ziim/Computer/ && tail -n4 0_Hanging.txt && cd $HOME/ziim/Donaghs/00_Home/ && tail -n4 0_Hanging.txt'
alias hg=histgrep
alias hj='history'
alias pss='python3 -m http.server 5000'
alias -g ra='ranger'
alias gett='$HOME/.scripts/get_password.sh'
alias synaptic='sudo synaptic'
alias tm="calc -d 'read qtime; qtime(0)'" # sudo apt install apcalc WINTER TIME
# alias tm="calc -d 'read qtime; qtime(1)'" # sudo apt install apcalc SUMMER TIME
alias tn='t | festival --tts'
alias tn='t | spd-say --pipe-mode'
alias speedtest="curl -o /dev/null http://speedtest.wdc01.softlayer.com/downloads/test10.zip"
alias wett='curl wttr.in/limerick'
alias def='~/.scripts/get_defs.sh ' # dictionary addition to $dict
alias -g define='BROWSER=w3m ddgr \!wordnik ' # ddgr is CLI DDGo and w3m is CLI browser
alias -g filepath='$HOME/.scripts/filename.sh'

# TODO
todo() { cd $HOME/TODO }
alias t="/home/donagh/TODO && ls"
alias td="/home/donagh/TODO && ls"
# Libre Office open TODO.ods / PROJECTS.ods
alias lot="/usr/sbin/libreoffice $HOME\/TODO\/TODO.ods &"
alias lop="/usr/sbin/libreoffice $HOME\/TODO\/PROJECTS.ods &"
# View todo / lists in the terminal
alias -g vt="/home/donagh/.scripts/todo-ods-to-csv.sh"
alias -g vp="/home/donagh/.scripts/projects-ods-to-csv.sh"
alias -g meta="/home/donagh/TODO/META"

# git shortcuts - but see $alias from zsh
alias cm='git commit -m '
alias gcm='git commit -m '
alias ggs='git status'
alias gst='git status'

# List directories / files
alias l.='ls -d .* --color=auto' # Show hidden files ##
alias ld='ls -la -d */'
alias lk='ls -1v'
alias ll='ls -la'
alias lls='ls | xargs -n1' # show ls in a single column
alias ls='ls --color=auto'
alias hp='ls --hide=*.py'
alias hh='cd ~/'
alias llk="ll | grep '^l.*'"
alias -g lf="ls | tr ' ' '\n'"
alias -g latest='ls -lt  **/*(.om[1,20])' # list latest 20 files edited

# python virtual envs shortcuts
alias pb='source $HOME/.virtualenvs/basic/bin/activate'
alias pd='source $HOME/.virtualenvs/django/bin/activate'
alias pf='source $HOME/.virtualenvs/flask/bin/activate'

# networking shortcuts
alias ipe='dig @resolver1.opendns.com ANY myip.opendns.com +short' # external ip -- better
alias ipi="ip -4 addr  | grep 192 | cut -d '/' -f1"
alias ping='ping -c3 '
alias png='ping -c4 8.8.8.8' # ping google to check if there is internet access

# zsh shortcuts
alias zcp='zmv -C'
alias zln='zmv -L'
alias zs='zsh_stats' # lists last 20 commands with statistics

# THE END
