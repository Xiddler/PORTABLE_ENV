# surround the selected text with two underscores e.g. __word__
# this is useful when using markdown

# contents = clipboard.get_selection()



# keyboard.sendkeys(contents+'&')

# new_contents = keyboard.sendkeys('__'+contents+'__')

# keyboard.send_keys(new_contents)
