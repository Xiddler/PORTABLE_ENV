# man
man man

# man sections

0 Header files (usually found in /usr/include) 
1 Executable programs or shell commands 
2 System calls (functions provided by the kernel) 
3 Library calls (functions within program libraries) 
4 Special files (usually found in /dev) 
5 File formats and conventions, e.g. /etc/passwd 
6 Games 
7 Miscellaneous (including macro packages and conventions), e.g. man(7), groff(7) 
8 System administration commands (usually only for root) 
9 Kernel routines [Non standard]

# Convert a man page to pdf
man -t bash | ps2pdf - bash.pdf

# View the ascii chart
man 7 ascii

# Read man pages with vim
vman <manpage>

# list all man pages on your system 
man -k ''

# if you just want to see the ones for high-level documentation (i.e. section 7), then
man -s 7 -k '

